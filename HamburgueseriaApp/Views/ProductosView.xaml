<UserControl x:Class="HamburgueseriaApp.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="24,0,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,14">
            <Button Content="➕ Agregar" Style="{StaticResource BotonAccion}" Command="{Binding AgregarCommand}"/>
            <Button Content="✏️ Editar" Style="{StaticResource BotonAccion}" Command="{Binding EditarCommand}"/>
            <Button Content="🗑️ Eliminar" Style="{StaticResource BotonAccion}" Command="{Binding EliminarCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Productos}" SelectedItem="{Binding Seleccionado}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="2*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding Precio, StringFormat='$ {0:N0}'}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="*"/>
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
