<Application x:Class="HamburgueseriaApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:HamburgueseriaApp.Helpers"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <ui:ThemesDictionary Theme="Dark"/>
                <ui:ControlsDictionary/>

            </ResourceDictionary.MergedDictionaries>


            <!-- ===================== CONVERTERS ===================== -->
            <helpers:BoolToCollapsedConverter x:Key="BoolToCollapsedConverter"/>
            <helpers:EnumToBoolConverter x:Key="EnumToBoolConverter"/>
            <helpers:CantidadAAnchoConverter x:Key="CantidadAAnchoConverter"/>
            <helpers:BoolToVisibleConverter x:Key="BoolToVisibleConverter"/>
            <helpers:MontoAAnchoConverter x:Key="MontoAAnchoConverter"/>
            <helpers:BoolAColorVariacionConverter x:Key="BoolAColorVariacionConverter"/>

            <!-- ===================== PALETA DE COLORES ===================== -->
            <SolidColorBrush x:Key="BrushFondo" Color="#1A1A1A"/>
            <SolidColorBrush x:Key="BrushPanel" Color="#232323"/>
            <SolidColorBrush x:Key="BrushPanelClaro" Color="#2E2E2E"/>
            <SolidColorBrush x:Key="BrushBorde" Color="#3A3A3A"/>
            <SolidColorBrush x:Key="BrushAmarillo" Color="#FFC72C"/>
            <SolidColorBrush x:Key="BrushRojo" Color="#E5352B"/>
            <SolidColorBrush x:Key="BrushRojoOscuro" Color="#B22620"/>
            <SolidColorBrush x:Key="BrushVerde" Color="#2FAE4E"/>
            <SolidColorBrush x:Key="BrushVerdeOscuro" Color="#1F8A3C"/>
            <SolidColorBrush x:Key="BrushTextoPrimario" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="BrushTextoSecundario" Color="#AEAEAE"/>



            <Style x:Key="CalendarOscuroStyle" TargetType="Calendar">
                <Setter Property="Background" Value="{StaticResource BrushPanel}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorde}"/>
            </Style>


            <!-- ===================== TIPOGRAFÍA BASE ===================== -->
            <Style TargetType="Window">
                <Setter Property="Background" Value="{StaticResource BrushFondo}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="TextElement.Foreground" Value="{StaticResource BrushTextoPrimario}"/>
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
            </Style>

            <Style TargetType="Label">
                <Setter Property="Foreground" Value="{StaticResource BrushTextoSecundario}"/>
            </Style>

            <!-- ===================== BOTÓN GRANDE (productos / navegación) ===================== -->
            <Style x:Key="BotonGrande" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="MinHeight" Value="60"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Fondo" Background="{TemplateBinding Background}" CornerRadius="10">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushAmarillo}"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushRojo}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Fondo" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== BOTÓN DE NAVEGACIÓN LATERAL ===================== -->
            <Style x:Key="BotonNav" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoSecundario}"/>
                <Setter Property="FontSize" Value="17"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="16,16"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Fondo" Background="{TemplateBinding Background}" CornerRadius="8" Margin="8,4">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" Margin="10,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                                    <Setter Property="Foreground" Value="{StaticResource BrushAmarillo}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== BOTÓN PRINCIPAL "COBRAR E IMPRIMIR" ===================== -->
            <Style x:Key="BotonCobrar" TargetType="Button" BasedOn="{StaticResource BotonGrande}">
                <Setter Property="Background" Value="{StaticResource BrushVerde}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="26"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="MinHeight" Value="90"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Fondo" Background="{TemplateBinding Background}" CornerRadius="14">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushVerdeOscuro}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== BOTÓN SECUNDARIO (chico: +, -, eliminar, acciones) ===================== -->
            <Style x:Key="BotonChico" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Width" Value="36"/>
                <Setter Property="Height" Value="36"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushAmarillo}"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== BOTÓN "ACCIÓN" (agregar / editar / eliminar en grillas) ===================== -->
            <Style x:Key="BotonAccion" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Padding" Value="18,10"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushAmarillo}"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== TEXTBOX ===================== -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="CaretBrush" Value="{StaticResource BrushAmarillo}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorde}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="FontSize" Value="16"/>
            </Style>

            <!-- ===================== COMBOBOX ===================== -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorde}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="Toggle"
                                  Focusable="False" ClickMode="Press"
                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{Binding Background, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        CornerRadius="6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="28"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path Grid.Column="1" Data="M0,0 L8,0 L4,5 Z"
                                              Fill="{StaticResource BrushTextoSecundario}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                       Content="{TemplateBinding SelectionBoxItem}"
                                       ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                       Margin="{TemplateBinding Padding}"
                                       HorizontalAlignment="Left" VerticalAlignment="Center"/>

                                <Popup x:Name="Popup" Placement="Bottom" AllowsTransparency="True"
                           Focusable="False" PopupAnimation="Slide"
                           IsOpen="{TemplateBinding IsDropDownOpen}">
                                    <Border Background="{StaticResource BrushPanel}"
                                BorderBrush="{StaticResource BrushBorde}"
                                BorderThickness="1" CornerRadius="8" Margin="0,4,0,0"
                                MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer Margin="4" SnapsToDevicePixels="True">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Fondo" Background="Transparent" CornerRadius="4" Margin="2,1"
                        Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushAmarillo}"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Fondo" Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ===================== DATAGRID ===================== -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="{StaticResource BrushPanel}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushTextoPrimario}"/>
                <Setter Property="RowBackground" Value="{StaticResource BrushPanel}"/>
                <Setter Property="AlternatingRowBackground" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorde}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BrushBorde}"/>
                <Setter Property="RowHeight" Value="40"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
            </Style>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource BrushPanelClaro}"/>
                <Setter Property="Foreground" Value="{StaticResource BrushAmarillo}"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Padding" Value="10,10"/>
                <Setter Property="BorderBrush" Value="{StaticResource BrushBorde}"/>
                <Setter Property="BorderThickness" Value="0,0,0,2"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
